#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$( cd "$( dirname "$(readlink -f "${BASH_SOURCE[0]}" )" )" &> /dev/null && pwd )"
CLOOSTERCTL_DIR="$SCRIPT_DIR/../cloosterctl"

cd "$CLOOSTERCTL_DIR"
exec yarn cloosterctl "$@"
