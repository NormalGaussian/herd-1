#!/bin/bash

set -euo pipefail

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
cd "$SCRIPT_DIR"

SAGGY="$SCRIPT_DIR/saggy"
KUBECONFIG_SOPS="$SCRIPT_DIR/../../secrets/kubeconfig.sops"

$SAGGY with "$KUBECONFIG_SOPS" -- kubectl --kubeconfig {} "${ARGS[@]}"
